var x;  // declaring x
x=20;  // assigning 20 to x
x=21;  // re-assigning of x
var x;  // re-declaring of x
console.log(x)
// ; is not necessary in javascript.

console.log("farrhad")

let y;  // declaring y
y=20;  // assigning 20 to y
y=21;  // re-assigning of y
// let y; re-declaring is not possible in let

const c=21; 
// re-declaring and re-assigning is not possible in const

let a=30;  // = is assignment operator
let sum=30+40;  // + is addition operator
let sub=30-40;  // - is subtract operator
let mul=30*40;  // * is multiplication operator
let div=30/40;  // / is division operator
let mod=30%4;  // % is modulus operator
let exp=2**4;  // ** is power/exponential operator. O/P- 2*2*2*2


